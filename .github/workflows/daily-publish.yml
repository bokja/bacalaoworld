name: Daily scheduled publish
on:
  schedule:
    - cron: '0 6 * * *' # 06:00 UTC = 07:00 CET
  workflow_dispatch: # allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
